<?phpget_header();?><section class="section-white single">	<div class="container-wrap">		<div class="container">		<div class="border-wrap page-item">			<?php				if ( have_posts() ) {					while ( have_posts() ) {						the_post();						//						// Post Content here						the_title('<h2 class="small-title">', '</h2>');						the_content();						//					} // end while				} // end if			?>			</div>		</div>	</div></section><?php get_footer(); ?>